description: Bad Request - Validation or input error
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - error
      properties:
        status:
          type: integer
          example: 400
        error:
          type: string
          example: Missing required fields
